management:
  endpoint:
    shutdown:
      enabled: true
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: ['health','env','info']
spring:
  application:
    name: provider
  profiles:
    active: first
  security:
    user:
      name: root
      password: root
ribbon:
  eureka:
    enabled: true
